const fs = require('fs'); const path = require('path'); function walk(dir) { fs.readdirSync(dir).forEach(f => { let p = path.join(dir, f); if (fs.statSync(p).isDirectory()) walk(p); else if (p.endsWith('.js') || p.endsWith('.html')) { let c = fs.readFileSync(p, 'utf8'); let nc = c.replace(/href=\"\/login\"/g, 'href=\"https://nutrisync-platform.onrender.com/login\"').replace(/Zvyicx_bc:\s*\"\/login\"/g, 'Zvyicx_bc: \"https://nutrisync-platform.onrender.com/login\"'); if (c !== nc) { fs.writeFileSync(p, nc); } } }); } walk('public');
